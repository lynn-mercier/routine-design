const Application = require('./src/application');
const GcpImageBucket = require('./src/gcp-image-bucket');
const LocalStorage = require('./src/local-storage');
const RoutineDesignTree = require('./src/routine-design-tree');
const WebPage = require('./src/web-page');

module.exports = {Application, GcpImageBucket, LocalStorage, RoutineDesignTree, WebPage};
